Missing function for CmdMessenger, once it is solved (pull request merged for CmdMessenger or missing define added or ...) delete this file!!!

It might be a toolchain bug or a configuration issue (e.g., may need to define _GNU_SOURCE, _BSD_SOURCE, ..)
see also https://github.com/platformio/platformio-core/issues/4028


#if defined(ARDUINO_ARCH_RENESAS)
// Safely copy two strings.
// From: https://stackoverflow.com/questions/18547251/when-i-use-strlcpy-function-in-c-the-compilor-give-me-an-error
size_t strlcpy(char *dst, const char *src, size_t size)
{
	// Length of source string
    size_t srclen;
    // Figure out how much room is needed...
    size--;
    srclen = strlen(src);

    // Copy the appropriate amount...
    if (srclen > size)
        srclen = size;
    memcpy(dst, src, srclen);
    dst[srclen] = '\0';
    return (srclen);
}
#endif

